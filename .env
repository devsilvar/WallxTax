# Production API URL — set in your deploy host's dashboard (Vercel / Railway).
# Leave unset for local dev (Vite proxies /api to localhost:3000).
VITE_API_BASE_URL=https://paymaytax-api-production.up.railway.app/api/v1
